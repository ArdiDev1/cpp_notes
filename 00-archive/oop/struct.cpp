//Structure

/*
    Creates a user-defined type through keyword struct
    Similar to a class
    Default access in a struct is public, Default access in a class is private
*/

//default access is public 
struct point
{
    int x;
    int y;
};
